<UserControl
	x:Class="CollectionManager.Views.ItemSetList"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="clr-namespace:CollectionManager.Views"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:vm="clr-namespace:CollectionManager.ViewModels"
	d:DataContext="{d:DesignInstance vm:MainWindowViewModel}"
	d:DesignHeight="450"
	d:DesignWidth="800"
	mc:Ignorable="d">
	<ListView ItemsSource="{Binding ItemSetList}" SelectedItem="{Binding CurrentItemSet.Value}">
		<ItemsControl.ItemTemplate>
			<DataTemplate>
				<TextBlock Text="{Binding Title.Value}" />
			</DataTemplate>
		</ItemsControl.ItemTemplate>
	</ListView>
</UserControl>
